# Offene Punkte — Stand 22.08.2026, 02:00

Alles Erledigte steht in der Git-Historie (14 Commits am 21./22.08.).
Hier steht nur, was **noch offen** ist, nach Schaden sortiert.

## Braucht Steffen

1. **Brevo-SMTP in Supabase eintragen.** Zugangsdaten und die zwei Stolpersteine
   (Username ist die SMTP-Login-Adresse, Passwort ist ein SMTP-Key, nicht der
   API-Key) stehen in `docs/MIGRATION.md`. Danach:
   - `Minimum interval between emails` von 60 auf 5 Sekunden
   - Die fertigen Vorlagen aus `supabase/templates/` per Dashboard einsetzen
     (`supabase config push` bricht mit dieser CLI-Version am Storage-Schema)
2. **In Claudes Browserfenster anmelden** statt auf dem Handy. Ohne Sitzung
   dort kann keine Runde am Tagebuch selbst belegt werden — jede Pruefung
   haengt sonst an Screenshots per Hand.

## Tagebuch — der Hero-Moment, hier lohnt die Tiefe

Runde 1 ist erledigt (lokales Datum, korrekte Tagesnummer). Offen:

3. **Vier Felder statt einer Frage.** Der Eintrag hat Text, Highlight, Notizen
   und Ausgaben. Das eigene Konzept sagt: *eine* Frage, ein Feld. Vier Felder
   sind ein Formular, kein Tagebuch — und der Grund, warum abends niemand
   schreibt.
4. **Keine Stimmung, keine Spuren, keine Fotos.** Genau die Bausteine, aus
   denen sich "der Tag schreibt sich selbst" ergibt.
5. **PDF-Export ungeprueft.** Laeuft ueber `window.print()`. Ob das auf dem
   iPhone ein Andenken ergibt oder einen Datenauszug, weiss niemand.
6. **Abendfrage um 20:00.** Der proaktive Moment. Braucht Mitteilungen, also
   die native Huelle — aber die Textseite laesst sich vorher entwerfen.

## Handwerk — gemessen, noch nicht behoben

7. **Skelette statt leerer Ladeflaechen** auf fuenf Routen.
8. **Dunkelmodus ist toter Code.** Die `.dark`-Tokens sind vollstaendig da,
   nichts setzt die Klasse je. Entweder aktivieren oder entfernen.
9. **Tabellenziffern** an den restlichen 13 Betragsstellen.
10. **Fokusring beim Tabben nicht verifiziert** — die Browsersteuerung konnte
    keine echten Tastenanschlaege senden. Von Hand pruefen.

## Design-System

11. **Jost und Newsreader statt Manrope, kialia-Palette statt der oklch-Werte.**
    Der erste Token (`--k-copper`) ist uebernommen. Der Rest ist ein Eingriff,
    der jeden Bildschirm gleichzeitig veraendert — erst mit Sicht auf alle
    Bildschirme anfassen. Handoff liegt unter `~/Downloads/kialia-handoff/`.

## Produkt — unveraendert der Hebel mit der groessten Wirkung

12. **Reisedaten liegen weiter im localStorage.** `trips` + `trip_members` in
    Supabase mit RLS ueber die Mitgliedschaft, dann echtes Teilen. Bewertung
    dazu: `docs/REVIEW_BOARD_2026-08-21.md` (27/100).

## Vertagt

13. **Widgets** — ausdruecklich vertagt, nicht vergessen. Braucht die native
    Huelle. Kandidaten stehen in
    `.claude/skills/app-store-ready/references/design-award.md`.
